[project]
name = "yt-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
    "pytube>=15.0.0",
]

[project.scripts]
server = "yt_mcp_server.main:main"
